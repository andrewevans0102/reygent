# Reygent

A CLI tool for Reygent.

## Install

```bash
npm install -g reygent
```

Or run directly with npx:

```bash
npx reygent
```

## Usage

```bash
# Show all available commands
reygent --help

# Show version
reygent --version
```
